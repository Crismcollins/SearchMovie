import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        backgroundColor: "green",
        borderRadius: 16,
        alignItems: "center",
        padding: 12
    },
    buttonText: {
        color: "white",
        fontSize: 16,
        fontWeight: "bold"
    }
});