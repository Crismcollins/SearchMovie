import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        justifyContent: "center",
        alignItems: "center",
        marginVertical: 12
    },
    button: {
        borderRadius: 6
    },
    text: {
        marginHorizontal: 8
    },
    disableButton: {
        opacity: 0.2
    }
});